version: '3.8'
services:
  nginx:
    deploy:
      labels:
        - traefik.enable=true
        - traefik.http.routers.worldcup2014.rule=Host(`$HOST`)
        - traefik.http.services.worldcup2014.loadbalancer.server.port=80
        # Activate https
        - traefik.http.routers.worldcup2014.tls=true
        - traefik.http.routers.worldcup2014.tls.certresolver=myresolver
        - traefik.http.routers.worldcup2014.middlewares=security@file
    networks:
      - gateway
      - default

  # admin:
  #   deploy:
  #     labels:
  #       - traefik.enable=true
  #       - traefik.http.routers.worldcup2014-admin.rule=Host(`$HOST_ADMIN`)
  #       - traefik.http.services.worldcup2014-admin.loadbalancer.server.port=8081
  #       # Activate https
  #       - traefik.http.routers.worldcup2014-admin.tls=true
  #       - traefik.http.routers.worldcup2014-admin.tls.certresolver=myresolver
  #       - traefik.http.routers.worldcup2014-admin.middlewares=security@file
  #   networks:
  #     - gateway
  #     - default

networks:
  gateway:
    external: true
    name: gateway